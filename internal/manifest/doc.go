// Package manifest implements the version model: FileMetadata, the
// immutable Version, VersionEdit encode/decode, and VersionSet, including
// the MANIFEST log and CURRENT file protocol.
//
// Depends on base, record, and vfs.
package manifest
